== OPERAÇÃO TERNARIA ==

. Essa operação aritmética se semelha bastante com o uso do 'if' já que a '?' é usada para se e o ':' é usado para senão, como no exemplo:

	int a = 3;
        int b = 4;
        String resultado = a<b ? "verdadeiro" : "falso";
        System.out.println(resultado); 